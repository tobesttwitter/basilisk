[build-system]
requires = ["hatchling==1.31.0"]
build-backend = "hatchling.build"

[project]
name = "basilisk-ai"
version = "2.0.2"
description = "LLM/AI Application Red Teaming Framework — Break Every Mind"
readme = "README.md"
license = {text = "AGPL-3.0"}
requires-python = ">=3.11"
authors = [
    {name = "Regaan", email = "support@rothackers.com"},
]
keywords = [
    "security", "ai", "llm", "red-team", "penetration-testing",
    "prompt-injection", "offensive-security", "vulnerability-scanner",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "Intended Audience :: Information Technology",
    "License :: OSI Approved :: GNU Affero General Public License v3",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Security",
    "Topic :: Software Development :: Testing",
]
dependencies = [
    "click>=8.3.3",
    "rich>=13.0.0",
    "httpx>=0.27.0",
    "anyio>=4.14.2",
    "litellm>=1.84.0",
    "pyyaml>=6.0",
    "fastapi>=0.141.1",
    "uvicorn[standard]>=0.30.0",
    "websockets>=12.0",
    "jinja2>=3.1.0",
    "pydantic>=2.7.0",
    "numpy>=1.26.0",
    "scikit-learn>=1.5.0",
    "tiktoken>=0.7.0",
    "cryptography>=48.0.1",
    # Security floors for transitive HTTP/rendering dependencies. Keeping
    # these explicit prevents older vulnerable versions in unlocked installs.
    "aiohttp>=3.14.3",
    "idna>=3.15",
    "pygments>=2.20.0",
    "requests>=2.33.0",
    "starlette>=1.3.1",
    "urllib3>=2.7.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23.0",
    "hypothesis>=6.100.0",
    "ruff>=0.4.0",
    "mypy>=1.10.0",
]
pdf = [
    "weasyprint>=62.0",
]
multimodal = [
    "Pillow>=10.0.0",
]
secrets = [
    "keyring>=25.0.0",
]
embeddings = [
    "sentence-transformers>=2.7.0",
]

[project.urls]
Homepage = "https://basilisk.rothackers.com"
Documentation = "https://basilisk.rothackers.com/docs"
Repository = "https://github.com/regaan/basilisk"
Issues = "https://github.com/regaan/basilisk/issues"

[project.scripts]
basilisk = "basilisk.cli.main:cli"

[tool.hatch.build.targets.wheel]
packages = ["basilisk"]

[tool.hatch.build.targets.sdist]
include = [
    "basilisk/",
    "README.md",
    "LICENSE",
    "pyproject.toml",
    "requirements.txt",
    "requirements.in",
    "requirements.lock",
    "requirements-dev.in",
    "requirements-dev.lock",
]

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "SIM"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.mypy]
python_version = "3.11"
strict = true
